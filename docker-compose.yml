services:
  github-agent:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-3800}:${PORT:-3800}"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data
    volumes:
      - agent-data:/app/data

volumes:
  agent-data:
